{
  "name": "@papercusp/kv-persist-indexeddb",
  "version": "0.0.1",
  "description": "IndexedDB implementation of the @papercusp/kv-persist contract. Client-only; degrades to a no-op when IndexedDB is unavailable (SSR / private mode).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run"
  },
  "peerDependencies": {
    "@papercusp/kv-persist": "*",
    "@papercusp/test-config": "*"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "vitest": "^4.1.5"
  }
}
