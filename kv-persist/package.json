{
  "name": "@restart/kv-persist",
  "version": "0.0.1",
  "description": "Storage-agnostic contract for persisting versioned key-value records (id + monotonic version). Backend-independent: implemented by @restart/kv-persist-indexeddb and consumable by any row/data store.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@papercusp/test-config": "*",
    "vitest": "^4.1.5"
  }
}
